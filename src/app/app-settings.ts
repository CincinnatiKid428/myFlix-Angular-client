//File: src/app/app-settings.ts

/**
 * This export will hold any global settings for the application outside of `app.config.ts`.
 */
export const AppSettings = {
  DEBUG_LOG: false // Set to true to enable debug application logging.
};